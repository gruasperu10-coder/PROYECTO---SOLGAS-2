import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solgas.clickshare',
  appName: 'Solgas ClickShare',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
